#if	!defined(CHANNEL_H)
#define	CHANNEL_H
#include <switch.h>

/*
extern switch_status_t radio_chan_on_init(switch_core_session_t *session);
extern switch_status_t radio_chan_on_hangup(switch_core_session_t *session);
extern switch_status_t radio_chan_on_destroy(switch_core_session_t *session);
extern switch_status_t radio_chan_on_routing(switch_core_session_t *session);
extern switch_status_t radio_chan_on_exchange_media(switch_core_session_t *session);
extern switch_status_t radio_chan_on_soft_execute(switch_core_session_t *session);
extern switch_call_cause_t chan_outgoing_call(switch_core_session_t *session, switch_event_t *var_event,
extern switch_status_t channel_read_frame(switch_core_session_t *session, switch_frame_t **frame, switch_io_flag_t flags, int stream_id);
extern switch_status_t channel_write_frame(switch_core_session_t *session, switch_frame_t *frame, switch_io_flag_t flags, int stream_id);
extern switch_status_t channel_kill_channel(switch_core_session_t *session, int sig);
*/

#endif	// !defined(CHANNEL_H)
